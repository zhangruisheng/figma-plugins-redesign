import { h } from 'preact';
import { HTMLProps } from '../../../types';
import { TextboxProps } from '../textbox';
export interface TextboxNumericProps extends TextboxProps {
    incrementBig?: number;
    integer?: boolean;
    maximum?: number;
    minimum?: number;
    incrementSmall?: number;
    value: null | string;
}
export declare function TextboxNumeric({ disabled, focused, icon, incrementBig, incrementSmall, integer, maximum, minimum, name, noBorder, onChange, placeholder, propagateEscapeKeyDown, value, ...rest }: HTMLProps<TextboxNumericProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=textbox-numeric.d.ts.map