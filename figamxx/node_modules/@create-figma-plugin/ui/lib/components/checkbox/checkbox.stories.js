/** @jsx h */
import { h } from 'preact';
import { useState } from 'preact/hooks';
import { Text } from '../text/text';
import { Checkbox } from './checkbox';
export default { title: 'Checkbox' };
export const Default = function () {
    const [state, setState] = useState({ foo: false });
    return (h(Checkbox, { name: "foo", onChange: setState, value: state.foo },
        h(Text, null, "Text")));
};
export const Disabled = function () {
    const [state, setState] = useState({ foo: false });
    return (h(Checkbox, { disabled: true, name: "foo", onChange: setState, value: state.foo },
        h(Text, null, "Text")));
};
export const Selected = function () {
    const [state, setState] = useState({ foo: true });
    return (h(Checkbox, { name: "foo", onChange: setState, value: state.foo },
        h(Text, null, "Text")));
};
export const DisabledSelected = function () {
    const [state, setState] = useState({ foo: true });
    return (h(Checkbox, { disabled: true, name: "foo", onChange: setState, value: state.foo },
        h(Text, null, "Text")));
};
//# sourceMappingURL=checkbox.stories.js.map