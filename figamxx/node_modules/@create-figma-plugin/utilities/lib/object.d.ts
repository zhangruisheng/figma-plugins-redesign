/**
 * Creates a deep copy of the given object.
 *
 * @category Object
 */
export declare function cloneObject(object: any): any;
/**
 * Performs a *shallow* comparison of arrays `a` and `b`.
 *
 * @returns Returns `true` if `a` and `b` are the same, else `false`.
 * @category Object
 */
export declare function compareArrays(a: Array<any>, b: Array<any>): boolean;
/**
 * Performs a *deep* comparison of objects `a` and `b`.
 *
 * @returns Returns `true` if `a` and `b` are the same, else `false`.
 * @category Object
 */
export declare function compareObjects(a: any, b: any): boolean;
/**
 * Extracts the specified list of `attributes` from the given `array` of
 * objects.
 *
 * @returns Returns an array of plain objects.
 * @category Object
 */
export declare function extractAttributes(array: Array<{
    [key: string]: any;
}>, attributes: Array<string>): Array<{
    [key: string]: any;
}>;
//# sourceMappingURL=object.d.ts.map