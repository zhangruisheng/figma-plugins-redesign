import { h } from 'preact';
import { HTMLProps } from '../../types';
export interface ButtonProps {
    children: preact.ComponentChildren;
    destructive?: boolean;
    disabled?: boolean;
    focused?: boolean;
    fullWidth?: boolean;
    loading?: boolean;
    onClick: EventListener;
    propagateEscapeKeyDown?: boolean;
    secondary?: boolean;
}
export declare function Button({ children, destructive, disabled, focused, fullWidth, loading, onClick, propagateEscapeKeyDown, secondary, ...rest }: HTMLProps<ButtonProps, HTMLButtonElement>): h.JSX.Element;
//# sourceMappingURL=button.d.ts.map