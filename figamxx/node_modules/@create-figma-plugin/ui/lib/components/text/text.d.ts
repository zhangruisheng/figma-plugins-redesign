import { h } from 'preact';
import { Alignment, HTMLProps } from '../../types';
export interface TextProps {
    children: preact.ComponentChildren;
    align?: Alignment;
    bold?: boolean;
    muted?: boolean;
    numeric?: boolean;
}
export declare function Text({ align, bold, children, muted, numeric, ...rest }: HTMLProps<TextProps, HTMLDivElement>): h.JSX.Element;
//# sourceMappingURL=text.d.ts.map