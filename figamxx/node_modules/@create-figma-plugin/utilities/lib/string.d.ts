/**
 * Adds a `✔` prefix to the given `message`.
 *
 * @category String
 */
export declare function formatSuccessMessage(message: string): string;
/**
 * Adds a `✘` prefix to the given `message`.
 *
 * @category String
 */
export declare function formatErrorMessage(message: string): string;
/**
 * Adds a `⚠` prefix to the given `message`.
 *
 * @category String
 */
export declare function formatWarningMessage(message: string): string;
/**
 * Returns `singular` if `number` is exactly `1`, else returns `plural`.
 * `plural` defaults to `${singular}s` if not specified.
 *
 * @category String
 */
export declare function pluralize(number: number, singular: string, plural?: string): string;
//# sourceMappingURL=string.d.ts.map