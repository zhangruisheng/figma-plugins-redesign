import { h } from 'preact';
import { HTMLProps, OnChange } from '../../types';
export interface SelectableItemProps {
    bold?: boolean;
    children: preact.ComponentChildren;
    disabled?: boolean;
    indent?: boolean;
    name: string;
    onChange: OnChange;
    onKeyDown?: EventListener;
    propagateEscapeKeyDown?: boolean;
    value: boolean;
}
export declare function SelectableItem({ bold, children, disabled, indent, name, onChange, onKeyDown, propagateEscapeKeyDown, value, ...rest }: HTMLProps<SelectableItemProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=selectable-item.d.ts.map