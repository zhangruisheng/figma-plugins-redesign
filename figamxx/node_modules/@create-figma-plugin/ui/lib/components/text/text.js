/** @jsx h */
import classnames from '@sindresorhus/class-names';
import { h } from 'preact';
import styles from './text.scss';
export function Text({ align = 'left', bold, children, muted, numeric, ...rest }) {
    return (h("div", Object.assign({}, rest, { class: classnames(styles.text, styles[align], bold === true ? styles.bold : null, muted === true ? styles.muted : null, numeric === true ? styles.numeric : null) }), children));
}
//# sourceMappingURL=text.js.map