import { h } from 'preact';
import { HTMLProps, OnChange } from '../../types';
export interface CheckboxProps {
    children: preact.ComponentChildren;
    disabled?: boolean;
    focused?: boolean;
    name: string;
    onChange: OnChange;
    onKeyDown?: EventListener;
    propagateEscapeKeyDown?: boolean;
    value: boolean;
}
export declare function Checkbox({ children, disabled, focused, name, onChange, onKeyDown, propagateEscapeKeyDown, value, ...rest }: HTMLProps<CheckboxProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=checkbox.d.ts.map