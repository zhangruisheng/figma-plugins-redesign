declare type Settings = {
    [key: string]: any;
};
/**
 * Loads your plugin’s `settings` (stored locally on the user’s computer).
 * Values in `settings` default to an optional `defaultSettings` object.
 *
 * @category Settings
 */
export declare function loadSettingsAsync(defaultSettings: Settings): Promise<Settings>;
/**
 * Saves the given `settings` for your plugin (stored locally on the user’s
 * computer).
 *
 * @category Settings
 */
export declare function saveSettingsAsync(settings: Settings): Promise<void>;
export {};
//# sourceMappingURL=settings.d.ts.map