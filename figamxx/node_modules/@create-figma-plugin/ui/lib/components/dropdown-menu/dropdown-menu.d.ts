import { h } from 'preact';
import { OnChange, Option } from '../../types';
export interface DropdownMenuProps {
    children: preact.ComponentChildren;
    focused?: boolean;
    fullWidth?: boolean;
    name: string;
    onChange: OnChange;
    options: Option[];
    right?: boolean;
    top?: boolean;
    value: null | string;
}
export declare type DropdownOption = Option;
export declare function DropdownMenu({ children, focused, fullWidth, name, onChange, options, right, top, value }: DropdownMenuProps): h.JSX.Element;
//# sourceMappingURL=dropdown-menu.d.ts.map