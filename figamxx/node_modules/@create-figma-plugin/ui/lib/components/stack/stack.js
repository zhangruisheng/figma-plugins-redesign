/** @jsx h */
import classnames from '@sindresorhus/class-names';
import { h, toChildArray } from 'preact';
import styles from './stack.scss';
export function Stack({ children, space = 'small', ...rest }) {
    return (h("div", Object.assign({}, rest, { class: classnames(styles[space]) }), toChildArray(children).map(function (element, index) {
        return (h("div", { key: index, class: styles.item }, element));
    })));
}
//# sourceMappingURL=stack.js.map