import { h } from 'preact';
import { HTMLProps, Option } from '../../../types';
import { TextboxProps } from '../textbox';
export declare type TextboxAutocompleteOption = Option;
export interface TextboxAutocompleteProps extends TextboxProps {
    filter?: boolean;
    options: TextboxAutocompleteOption[];
    strict?: boolean;
    top?: boolean;
}
export declare function TextboxAutocomplete({ disabled, focused, filter, icon, name, noBorder, onChange, options, placeholder, strict, top, value: committedValue, ...rest }: HTMLProps<TextboxAutocompleteProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=textbox-autocomplete.d.ts.map