/** @jsx h */
import classnames from '@sindresorhus/class-names';
import { h, toChildArray } from 'preact';
import styles from './columns.scss';
export function Columns({ children, space, ...rest }) {
    return (h("div", Object.assign({}, rest, { class: classnames(styles.columns, typeof space === 'undefined' ? null : styles[space]) }), toChildArray(children).map(function (element, index) {
        return (h("div", { key: index, class: styles.column }, element));
    })));
}
//# sourceMappingURL=columns.js.map