import { h } from 'preact';
import { HTMLProps, OnChange, Space } from '../../types';
export interface RadioButtonsProps {
    disabled?: boolean;
    focused?: boolean;
    name: string;
    onChange: OnChange;
    options: RadioButtonsOption[];
    propagateEscapeKeyDown?: boolean;
    space?: Space;
    value: null | string;
}
interface RadioButtonsOption {
    disabled?: boolean;
    text?: preact.ComponentChildren;
    value: null | string;
}
export declare function RadioButtons({ disabled, focused, name, onChange, options, propagateEscapeKeyDown, space, value, ...rest }: HTMLProps<RadioButtonsProps, HTMLInputElement>): h.JSX.Element;
export {};
//# sourceMappingURL=radio-buttons.d.ts.map