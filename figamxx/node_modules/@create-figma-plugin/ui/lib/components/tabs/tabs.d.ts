/** @jsx h */
import { h } from 'preact';
import { HTMLProps, OnChange } from '../../types';
export interface TabsProps {
    focused?: boolean;
    name: string;
    onChange: OnChange;
    options: TabsOption[];
    propagateEscapeKeyDown?: boolean;
    value: null | string;
}
export interface TabsOption {
    disabled?: boolean;
    text?: preact.ComponentChildren;
    value: null | string;
    view: preact.ComponentChildren;
}
export declare function Tabs({ focused, name, onChange, options, propagateEscapeKeyDown, value, ...rest }: HTMLProps<TabsProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=tabs.d.ts.map