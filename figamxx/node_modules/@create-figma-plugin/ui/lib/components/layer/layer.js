/** @jsx h */
import classnames from '@sindresorhus/class-names';
import { h } from 'preact';
import { componentIcon } from '../icon/icons/component-icon';
import { frameIcon } from '../icon/icons/frame-icon';
import styles from './layer.scss';
const icons = {
    component: componentIcon,
    frame: frameIcon
};
export function Layer({ children, onClick, pageName, selected, type, ...rest }) {
    return (h("div", Object.assign({}, rest, { class: classnames(styles[type], styles.layer, selected === true ? styles.selected : null), onClick: onClick }),
        h("div", { class: styles.icon }, icons[type]),
        h("div", { class: styles.layerName }, children),
        typeof pageName !== 'undefined' ? (h("div", { class: styles.pageName }, pageName)) : null));
}
//# sourceMappingURL=layer.js.map