/** @jsx h */
import { h } from 'preact';
import { useState } from 'preact/hooks';
import { Text } from '../text/text';
import { Tabs } from './tabs';
export default { title: 'Tabs' };
export const Default = function () {
    const [state, setState] = useState({ foo: null });
    return (h(Tabs, { name: "foo", onChange: setState, options: [
            { value: 'foo', view: h(Text, null, "Foo") },
            { value: 'bar', view: h(Text, null, "Bar") },
            { value: 'baz', view: h(Text, null, "Baz") }
        ], value: state.foo }));
};
export const WithSelectedOption = function () {
    const [state, setState] = useState({ foo: 'bar' });
    return (h(Tabs, { name: "foo", onChange: setState, options: [
            { value: 'foo', view: h(Text, null, "Foo") },
            { value: 'bar', view: h(Text, null, "Bar") },
            { value: 'baz', view: h(Text, null, "Baz") }
        ], value: state.foo }));
};
//# sourceMappingURL=tabs.stories.js.map