import { h } from 'preact';
import { HTMLProps, OnChange } from '../../types';
export interface TextboxProps {
    disabled?: boolean;
    focused?: boolean;
    icon?: preact.ComponentChildren;
    name: string;
    noBorder?: boolean;
    onChange: OnChange;
    placeholder?: string;
    propagateEscapeKeyDown?: boolean;
    type?: 'text' | 'password';
    value: null | string;
}
export declare function Textbox({ disabled, focused, icon, name, noBorder, onChange, placeholder, propagateEscapeKeyDown, type, value, ...rest }: HTMLProps<TextboxProps, HTMLInputElement>): h.JSX.Element;
//# sourceMappingURL=textbox.d.ts.map