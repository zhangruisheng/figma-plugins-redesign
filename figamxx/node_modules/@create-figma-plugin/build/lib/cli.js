#!/usr/bin/env node
"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sade = require("sade");
const build_async_1 = require("./build-async");
const watch_1 = require("./watch");
sade('build-figma-plugin', true)
    .describe('Build a Figma plugin, with support for multiple menu commands that each have their own UI implementation')
    .option('-d, --dev', 'Create an unminified bundle with sourcemaps')
    .option('-w, --watch', 'Rebuild the plugin on code changes')
    .action(async function (options) {
    const { dev, w } = options;
    if (w === true) {
        watch_1.watch();
        return;
    }
    await build_async_1.buildAsync(dev, true);
})
    .parse(process.argv);
//# sourceMappingURL=cli.js.map